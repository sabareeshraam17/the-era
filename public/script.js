const PRODUCTS=[{id:"baggy-combo",name:"Baggy Combo",price:1480,image:"assets/combo.jpg",tag:"BESTSELLER"},{id:"baggy-black-denim",name:"Baggy Black Light Washed Denim",price:899,image:"assets/denim.jpg",tag:"NEW"},{id:"slim-yellow-stripe-shirt",name:"Slim Fit Yellow Stripe Shirt",price:599,image:"assets/shirt.jpg",tag:"ESSENTIAL"}];
const CART_KEY="theEraCart",ORDER_KEY="theEraLastOrder";function getCart(){try{return JSON.parse(localStorage.getItem(CART_KEY))||[]}catch{return[]}}function saveCart(c){localStorage.setItem(CART_KEY,JSON.stringify(c));updateCartCount()}function money(n){return "₹"+Number(n).toLocaleString("en-IN")}function updateCartCount(){const n=getCart().reduce((s,i)=>s+i.qty,0);document.querySelectorAll("[data-cart-count]").forEach(e=>e.textContent=n)}function toast(m){const t=document.querySelector(".toast");if(!t)return;t.textContent=m;t.classList.add("show");setTimeout(()=>t.classList.remove("show"),1800)}function addToCart(id){const p=PRODUCTS.find(x=>x.id===id),c=getCart(),i=c.find(x=>x.id===id);if(i)i.qty++;else c.push({id,qty:1});saveCart(c);toast(p.name+" added to cart")}function removeFromCart(id){saveCart(getCart().filter(i=>i.id!==id));renderOrderSummary()}function changeQty(id,d){const c=getCart(),i=c.find(x=>x.id===id);if(!i)return;i.qty+=d;if(i.qty<=0)return removeFromCart(id);saveCart(c);renderOrderSummary()}function renderProducts(){const g=document.querySelector("[data-products]");if(!g)return;g.innerHTML=PRODUCTS.map(p=>`<article class="card"><div class="card-image"><img src="${p.image}" alt="${p.name}"></div><div class="card-body"><span class="tag">${p.tag}</span><div class="card-title">${p.name}</div><div class="card-price">${money(p.price)}</div><div class="card-actions"><button class="btn full" onclick="addToCart('${p.id}')">Add to cart</button></div></div></article>`).join("")}
function renderOrderSummary(){const b=document.querySelector("[data-summary]");if(!b)return;const c=getCart();if(!c.length){b.innerHTML='<div class="empty">Your cart is empty.<br><br><a class="btn alt" href="index.html#shop">Shop collection</a></div>';return}let total=0;b.innerHTML=c.map(i=>{const p=PRODUCTS.find(x=>x.id===i.id);total+=p.price*i.qty;return `<div class="summary-item"><img src="${p.image}" alt=""><div class="summary-info"><strong>${p.name}</strong><span>${money(p.price)} × ${i.qty}</span><div style="display:flex;gap:6px;margin-top:8px"><button class="btn alt" style="padding:5px 9px" onclick="changeQty('${p.id}',-1)">−</button><button class="btn alt" style="padding:5px 9px" onclick="changeQty('${p.id}',1)">+</button><button class="btn alt" style="padding:5px 9px" onclick="removeFromCart('${p.id}')">Remove</button></div></div></div>`}).join("")+`<div class="total"><span>Total</span><span>${money(total)}</span></div>`}
async function submitOrder(e){e.preventDefault();const c=getCart();if(!c.length){toast("Add a product first");return}const f=e.target;if(!f.checkValidity()){f.reportValidity();return}const method=f.querySelector('input[name="payment"]:checked')?.value,data=Object.fromEntries(new FormData(f).entries());if(method==="upi"&&!data.upiId){document.querySelector("#upiId").focus();toast("Enter your UPI ID");return}const payload={customer:{name:data.name,phone:data.phone,email:data.email,address:data.address,city:data.city,pin:data.pin},payment:method,upiId:data.upiId||"",items:c.map(i=>({id:i.id,qty:i.qty}))};try{const res=await fetch("/api/orders",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const out=await res.json();if(!res.ok)throw new Error(out.error||"Unable to place order");localStorage.setItem(ORDER_KEY,JSON.stringify(out.order));localStorage.removeItem(CART_KEY);location.href="success.html"}catch(err){toast(err.message)}}
async function loadSuccess(){const o=document.querySelector("[data-success]");if(!o)return;const local=JSON.parse(localStorage.getItem(ORDER_KEY)||"null");if(!local){o.innerHTML='<div class="empty">No recent order found.</div>';return}const names=(local.items||[]).map(i=>{const p=PRODUCTS.find(x=>x.id===i.id);return p?`${p.name} × ${i.qty}`:"Item"}).join(", ");o.innerHTML=`<div class="success"><div class="success-mark">✓</div><div class="kicker">ORDER CONFIRMED</div><h1 class="page-title">Thank you.</h1><p>Your order has been placed successfully. Your order is now visible to THE ERA owner for processing.</p><div class="order-id">${local.orderId}</div><div class="panel" style="text-align:left"><strong>Items</strong><p style="color:#666;margin:7px 0 18px">${names}</p><div class="total"><span>Total</span><span>${money(local.total)}</span></div><div class="notice"><strong>Payment:</strong> ${local.payment==="cod"?"Cash on Delivery":"UPI"}</div></div><br><a class="btn" href="index.html">Continue shopping</a></div>`}
document.addEventListener("DOMContentLoaded",()=>{updateCartCount();renderProducts();renderOrderSummary();loadSuccess();document.querySelectorAll('input[name="payment"]').forEach(r=>r.addEventListener("change",()=>{const w=document.querySelector("[data-upi-wrap]");if(w)w.style.display=r.value==="upi"&&r.checked?"block":"none"}));const f=document.querySelector("[data-order-form]");if(f)f.addEventListener("submit",submitOrder)});
